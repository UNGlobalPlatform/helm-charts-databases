apiVersion: v2
name: mariadb-op
description: MariaDB is a SQL database server. It's MySQL but libre. This chart provisions the database through mariadb-operator (requires the operator to be installed cluster-wide).
icon: https://mariadb.com/wp-content/uploads/2019/11/mariadb-logo-vert_blue-transparent.png
keywords:
  - SQL
  - Database
  - MariaDB
home: https://mariadb.org
sources:
  - https://github.com/mariadb-operator/mariadb-operator
  - https://github.com/UNGlobalPlatform/helm-charts-databases
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0
